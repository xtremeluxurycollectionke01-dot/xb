// next.config.js
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "images.nsplash.com",
      "maps.googleapis.com",
      "ui-avatars.com", 
    
    ],
  },
};

export default nextConfig;